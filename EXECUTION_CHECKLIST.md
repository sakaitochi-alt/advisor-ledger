# LINE Harness シナリオ実行チェックリスト

## ✅ 実行前確認

### 1. LINE Harness 環境
- [ ] LINE Harness インスタンスが起動している（自ホスト or クラウド版）
- [ ] APIが正常に動作している
- [ ] ワークスペースが作成されている（ID: `1f08753e8b48`）

### 2. LINE Official Account
- [ ] LINE Official Account が作成済み
- [ ] LINE Messaging API が有効化されている
- [ ] Channel Access Token が取得済み

### 3. 必要な認証情報
- [ ] **LINE Harness APIトークン** 
  - 取得場所：LINE Harness ダッシュボード → 設定 → API キー
  - 形式例：`hxxx-xxxx-xxxx-xxxx`

- [ ] **APIベースURL**
  - 自ホスト版：`http://localhost:3000/api` (デフォルト)
  - クラウド版：提供されたURL

### 4. ローカル環境
- [ ] Node.js がインストールされている（v14以上推奨）
- [ ] npm がインストールされている
- [ ] 作業ディレクトリ：`/home/user/advisor-ledger`

---

## 🚀 実行ステップ

### ステップ1：パッケージインストール

```bash
cd /home/user/advisor-ledger
npm install axios
```

### ステップ2：スクリプト実行

```bash
node line-harness-scenario.js
```

### ステップ3：対話的入力

スクリプトが以下を順に尋ねます。上記の確認情報を用意して入力：

```
1️⃣  API Base URL [http://localhost:3000/api]: 
   → APIベースURLを入力（または Enter でデフォルト使用）

2️⃣  API Token: 
   → APIトークンを入力（ペースト推奨）

3️⃣  Workspace ID [1f08753e8b48]: 
   → ワークスペースID（または Enter で既存値使用）

4️⃣  Account ID [2011673077]: 
   → アカウントID（または Enter で既存値使用）

5️⃣  物件情報を編集しますか？ (y/n) [n]: 
   → 物件情報を変更する場合のみ 'y' を入力

6️⃣  このシナリオで実装しますか？ (y/n) [y]: 
   → 最終確認。問題なければ 'y'
```

---

## 📊 実行結果

### ✅ 成功時の出力例

```
═══════════════════════════════════════════════════════════
  ⚙️  シナリオ実装中
═══════════════════════════════════════════════════════════

✅ シナリオ作成完了: scenario_xyz123...
✅ 初期メッセージステップ追加完了
✅ 資料リクエスト分岐追加完了
✅ 日時選択分岐追加完了

✨ 実装完了

✅ シナリオID: scenario_xyz123...

次のステップ:
  1. LINE Harnessダッシュボードでシナリオを確認
  2. LINE Official Accountに関連付け
  3. テスト配信を実施
```

### ❌ エラー時の対応

| エラーメッセージ | 原因 | 対応 |
|---|---|---|
| `API接続失敗` | LINE Harnessが起動していない | インスタンス起動確認 |
| `Authorization Bearer token not found` | APIトークンが無効 | トークン再取得 |
| `Scenario not found` | ワークスペースIDが間違っている | ダッシュボードで確認 |
| `ECONNREFUSED` | APIに接続できない | URL・ファイアウォール確認 |

---

## 📋 次のステップ（実装後）

### 1. ダッシュボード確認（5分）
```
LINE Harness ダッシュボード
 → シナリオ一覧
 → 「岩国市立石町4丁目 売買土地 自動応答」を確認
```

### 2. LINE Official Account 関連付け（10分）
```
LINE Harness ダッシュボード
 → 設定 → LINE Official Account
 → 対象アカウントを選択
 → 上記シナリオを割り当て
```

### 3. テスト配信（15分）
```
手順：
1. テストユーザーでLINE Official Accountを友だち追加
2. 「【物件名】へのお問い合わせ…」が自動送信される
3. 各ボタンをクリックして動作確認
   - ① 資料リンク送信の確認
   - ② フォーム表示の確認
   - ③ フォーム送信の確認
```

### 4. 本番配信（準備完了時）
```
LINE Harness ダッシュボード
 → シナリオ詳細 → 「配信を有効化」
 → LINE Official Account公開開始
```

---

## 🔧 トラブルシューティング

### Q:「API接続確認完了」と出ましたが、その後エラーが出ました
**A:** 認証には成功しましたが、シナリオ作成時にエラーが発生しています。
- ワークスペースIDが正しいか確認
- LINE Harness ダッシュボードでワークスペースが有効か確認

### Q: スクリプトが途中で止まります
**A:** タイムアウトの可能性があります。
- ネットワーク接続を確認
- LINE Harness APIのレスポンスを確認（ダッシュボールのログを見る）
- 30秒以上待ってから再実行

### Q: 物件情報を変更したい
**A:** スクリプト実行時に 'y' を入力することで編集可能です。
- スクリプトを再実行
- 「物件情報を編集しますか？」で 'y' 入力
- 各項目を編集

---

## 📞 サポート

問題が解決しない場合：
1. `LINE_HARNESS_SETUP.md` の「トラブルシューティング」を確認
2. LINE Harness [GitHub Issues](https://github.com/Shudesu/line-harness-oss/issues) を検索
3. APIトークン・ワークスペースIDの確認

---

## 🎯 実装の成功指標

- [ ] スクリプト実行でシナリオが作成される
- [ ] ダッシュボードにシナリオが表示される
- [ ] テスト友だち追加で初期メッセージが送信される
- [ ] 各ボタン分岐が正常に動作する
- [ ] フォームが営業日時のみを受け付ける
